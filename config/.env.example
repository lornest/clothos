# ClothOS node environment — copy to .env.node-a, .env.node-b, etc.
ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...       # Required if memory embedding uses OpenAI
# GH_TOKEN=ghp_...            # GitHub fine-grained PAT for git/gh CLI access
# CLOTHOS_CHANNELS__ADAPTORS__TELEGRAM__SETTINGS__BOTTOKEN=...
# WHATSAPP_AUTH_TOKEN=...     # If this node runs a WhatsApp channel
